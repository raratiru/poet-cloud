#!/bin/bash

touch /tmp/ansible_maintenance_wait

cd {{ ansible_user_dir }}/00_server/{{ server_domain_name }}
eval "$({{ ansible_user_dir }}/.local/bin/shdotenv --overload)"

{{ ansible_user_dir }}/.local/bin/poet_server_make_ready for_main_server
# TODO: The above line should only apply to the main server (web)

rm /tmp/ansible_maintenance_wait